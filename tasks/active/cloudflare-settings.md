# Prompt
Looking for help with the cloudflare settings to harden the server.

## The current settings
* Some of the current settings will be uploaded into tmp/ as screenshots.
* I am using "Ashburn" region.

## Goal
* To configure the server in a way to utilize security features of Cloudflare while making sure the clients can access the server and it performs well.

## Clients
* The bulk of the clients accessing the server is going to be AI agent. Make sure the server setttings are such as to optimize for agent client traffic (caching settings, crawler hints?, ...)

## Cloudflare Web UI Screens
1. DNS settings: `cf_dns.png`, `cf_dns_2.png`
2. SSL/TLS: `cf_tls.png`
3. Security page -- too many to capture in a shot. Please provide a list of recommended configuration
4. Rules: `cf_rules.png`
5. Web3: "Subscribe to web3 gateways"?
6. Any other pages/settings that are important
