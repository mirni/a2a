# v1.2.4 Audit P1/P2 Follow-ups

## Context

The P0 items from `reports/external/v1.2.4/multi-persona-audit-v1.2.4-2026-04-11.md`
shipped in PR `fix/v1.2.4-audit-p0` (see `reports/audit-response-v1.2.4.md`).
This task tracks the remaining HIGH/MEDIUM findings and the test-
strategy upgrades the plan called out, to be picked up next cycle.

## Deferred from the single-PR plan

### Test infrastructure

- **T-1** Multi-tenant fixture for `gateway/tests/conftest.py` — add
  `multi_tenant_keys` fixture that provisions N=5 distinct agents
  (2 free, 2 pro, 1 admin). Add `@pytest.mark.requires_multi_tenant`
  and a CI guard that fails if `tests/security/` reuses the
  legacy single-agent fixture.
- **T-2** Route enumeration contract — new
  `gateway/tests/test_route_enumeration.py` that walks `app.routes`
  and asserts every route has a targeted test file.
- **T-3** Sandbox parity CI job — `tests/sandbox/` directory with:
  - `test_audit_probe.py`
  - `test_infra_admin_gate.py`
  - `test_execute_410.py`
  - `test_idempotency_collision.py`
  - `test_budget_cap.py`
  - `test_deposit_limits.py`
  - `test_health_latency.py` (SLO: p50 < 300ms)
  Requires secrets: `SANDBOX_AUDIT_FREE_KEY`, `SANDBOX_AUDIT_PRO_KEY`,
  `SANDBOX_AUDIT_ADMIN_KEY` — see `tasks/backlog/sandbox-audit-keys.md`.
- **T-4** Real gatekeeper in CI — extend existing gatekeeper test
  to hit the real Lambda (`VERIFIER_AUTH_MODE=iam`) in CI only.
- **T-5** OpenAPI schema diff gate — `scripts/dump_openapi.py` and
  baseline snapshot; CI fails on removal or type change without a
  `deprecation:` key in the PR body.

### P1/P2 product findings (to be triaged)

- SSE heartbeat
- ML metrics allow-list
- USD→ETH conversion
- Subscription reactivation
- Additional SDK paper cuts

## References

- Plan: previous session transcript (v1.2.4 Audit P0 Remediation plan)
- PR: `fix/v1.2.4-audit-p0`
- Response doc: `reports/audit-response-v1.2.4.md`
