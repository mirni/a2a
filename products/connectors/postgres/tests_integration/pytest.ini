[pytest]
# Standalone pytest config for integration tests — overrides the
# connector's pyproject.toml which pins testpaths=["tests"].
asyncio_mode = auto
testpaths = .
markers =
    slow: marks tests that take >1s
